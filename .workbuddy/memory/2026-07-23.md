# 2026-07-23 工作日志

## FeiyangAgent 项目优化（后端为主）

完成 8 项优化，全部通过编译 + uvicorn 冒烟验证（/api/config、monitor-logs、sniper/dashboard、trades 均正常）：

1. **严重 Bug 修复**：`backend/app.py` 缺 `import json`，导致 `load_signals_state` 在 `last_signals.json` 存在时必然 NameError，信号去重/防重复推送机制完全失效。已补导入并验证状态读写闭环。
2. **Bug 修复**：`backend/sniper_engine.py` `fetch_live_usdt_balance` 调用不存在的方法 `self.save_state()`（实为 `_save_state`），实盘余额永不持久化且异常被静默吞掉。已修正。
3. **性能**：`data_fetcher.py` 新增线程安全单例工厂 `get_data_fetcher(exchange_id)`；此前 fast_price_check_loop 每 10 秒新建 4 个 ccxt 交易所对象 + 代理探测 + 改全局环境变量。代理 env 现在每进程只设置一次（`_proxy_env_applied` 双检锁）。
4. **性能**：10 秒价格轮询从「每币一次 1h K 线」改为 `fetch_latest_prices`（批量 `fetch_tickers` 一次请求，失败回退 K 线），API 调用量降为原来约 1/30。
5. **性能**：`get_chart_data` 去除 `iterrows()`，改为列操作 + `to_dict('records')`，显式 int/float 转换保证 JSON 序列化与旧输出一致（NaN 省略）。
6. **性能**：`/api/market` 增加 30s TTL 缓存（`MARKET_CACHE_TTL_SECONDS`）；`/api/analyze` 传 `force_refresh=True` 绕过缓存保证 LLM 拿到新数据。
7. **安全**：CORS 从 `allow_origins=["*"] + credentials` 改为 `allow_origin_regex` 仅放行 localhost/127.0.0.1 任意端口（原配置既不符合规范又让任意本地网页可读写 API Key）。
8. **健壮性**：`main.py` GUI 启动从 `time.sleep(0.8)` 改为轮询 `/api/config` 健康检查（最长 ~10s），避免慢机器白屏。

验证后清理了测试生成的 `last_signals.json` 假数据。

## 待跟进（未做）
- 前端 React 层面未改动（轮询逻辑、组件优化可作为下一步）
- `@app.on_event("startup")` 已废弃，未来可迁移到 lifespan（当前 FastAPI 版本仍兼容）
- 项目无单元测试体系，可考虑补 pytest
- 实盘挂单依赖 ccxt binance 的 stopLoss/takeProfit 附加参数（未经真实交易所验证）；若 App 崩溃/断网，本地双保险失效期间仓位无保护——可考虑成交后补挂交易所侧 STOP_MARKET 保护单

## 交易链路优化（预测/挂单/开单/风控，26 项断言全过）

用户目标"用这个赚钱"，本次聚焦让风控模型真实有效、模拟战绩可信：

1. **杠杆安全帽（最关键）**：`calculate_trade_params` 新增 `max_safe_lev = floor(0.75 / 止损距离%)`，70x+3%止损的旧组合会在止损前先被强平（亏 100% 保证金），现在杠杆自动降级（3%→25x），2% 风控预算真正生效。
2. **TP1 保本平仓记账 Bug 修复**：旧代码保本离场时 `pnl_usd = margin * 0`（赋值+全额保证金），TP1 已落袋利润被抹零、该单从胜率统计消失。改为剩余腿累加记账（多空两侧）。
3. **挂单成交语义贴合真实限价单**：旧逻辑价格进入区间任意位置即按当前价成交（系统性扭曲模拟战绩）；改为穿越 `planned_entry` 才按 `planned_entry` 成交；价格先穿止损则挂单作废（结构破坏）。
4. **手续费/滑点模型**：config 新增 `taker_fee_rate 0.0005 / maker_fee_rate 0.0002 / slippage_rate 0.0005`（旧 trades.json 自动合并默认值）；开仓(maker)、TP1/终极止盈(taker)、止损/手动平仓(taker+滑点)全腿扣费，记入 `trade["fees_usd"]`，余额按净额。实测 2% 计划止损实际亏损 -212.56（含 ~12.6 成本），模拟战绩从此可信。
5. **预测层**：`process_new_signal` 硬校验信号几何（多头 SL<入场下限 且 TP1>入场上限，空头反之），非法信号直接拒单（旧代码仅 log warning 照样开单）；`analyze()` 解析失败自动重试一次（附纠错指令）；指标新增 ATR(14) 进 payload，系统提示词要求止损距离 ≥1 倍 ATR。

验证：26/26 断言通过（临时目录实例化引擎全生命周期：杠杆帽、拒单、成交价、保本利润存活、余额一致性、跳空作废、止损净额）；uvicorn 冒烟通过且费率配置已加载。

## 第三轮：熔断/过期/回撤指标/实盘保护单（19 项断言全过）

用户要求"能赚钱的最完美狙击系统"，自主决策实施：

1. **日内回撤熔断器**：config 新增 `circuit_breaker_enabled/daily_max_loss_percent(默认6%)`；state 新增 `daily` 跟踪器（日期/起始余额/halted/notified）。当日实现亏损超阈值 → 拒开新单 + 撤销全部挂单（含实盘交易所单）+ 推送警报（每日一次），次日自动复位。注意：熔断在引擎 tick 开头评估，亏损对其可见最晚延迟一个 tick（生产 10 秒）。
2. **挂单过期**：`pending_ttl_hours`(默认 24h)，check_market_prices 中过期自动撤单（实盘同步）。
3. **状态锁**：SniperEngine 加 `threading.RLock`，process_new_signal/check_market_prices/close_position_manually/reset_paper_data/update_config 全部包装（impl 模式避免重排代码）。
4. **仪表盘新指标**：`max_drawdown_usd/percent`、`equity_curve`(最近50点)、`total_fees_usd`、`circuit_breaker` 状态块；基准用隐含起始权益 = 当前余额 − 累计净盈亏（paper/live 通用）。前端 SniperDashboard 加熔断红色横幅 + 最大回撤卡 + 熔断与成本卡 + 配置弹窗两个新字段；`/api/sniper/config` 模型同步放行新字段（含三个费率）。前端已 vite build。
5. **实盘交易所侧保护单（best-effort）**：fill 检测后用 `triggerPrice+reduceOnly` 补挂 STOP 止损单（`protective_sl_order_id` 记录），本地平仓时同步撤销；挂设失败推送高危警告提示手动设止损。未经真实交易所验证，全 try/except 兜底。

验证：19/19 断言（熔断触发/拒单/撤挂单/次日复位/TTL 过期/回撤与权益曲线与手续费数值/配置更新）；uvicorn 冒烟 + 配置 API 读写新字段通过（测试后已恢复默认值 6%/24h）。

## 第四轮：完全体 —— 历史回测框架 + 资金费率（17 项断言全过）

用户要求"开发好完全体"，交付最后两块拼图：

1. **历史回测引擎 `backend/backtest.py`（核心大件）**：Walk-forward 逐根回放——一次性拉全周期历史 K 线，每个信号点按时间戳切片→指标→payload→LLM 诊断→驱动独立 SniperEngine 实例（临时目录，关闭熔断/TTL/推送，时间轴是模拟的）执行。零重复实现：成交/TP1/保本/止损/手续费/杠杆帽全部走生产代码路径。后台线程 + 进度上报 + 取消；analyzer/fetcher 可注入（测试无需网络与真实 LLM）。结果含净值/胜率/PF/最大回撤/手续费/权益曲线/成交明细/口径说明。已知局限已在结果中声明：1h 收盘价刻度（插针不可见）、资金费不计入（真实时钟）。
2. **回测 API + CLI**：`POST /api/backtest/start`（symbol/days≤90/step_hours/max_llm_calls≤500/initial_balance）、`status/result/stop` 端点；`main.py --backtest --bt-days --bt-step --bt-calls --bt-balance` 同步运行打印摘要。
3. **资金费率模型**：config `funding_rate_per_8h`(0.0001)；持仓跨 UTC 8h 时点按剩余名义价值扣费，balance/pnl_usd/fees_usd 同步扣减，胜率统计含全部成本；同 epoch 不重复扣。
4. **前端「📈 历史回测」Tab**：BacktestPanel.jsx——参数表单+成本预估提示、进度条轮询(3s)、结果五卡（净收益/胜率PF/最大回撤/成本/调用数）、权益曲线 canvas、成交明细表、口径说明；App.jsx 接入新 Tab。已 vite build。
5. README 同步更新（特性、结构、CLI 用法）。

排障记录：测试桩教训——① FakeFetcher 需按 config 周期键返回；② 合成数据时间戳单位重复×1000 导致切片全空（引擎 warmup guard 正确拦截）；③ clean_and_compress 依赖 DataFetcher 添加的 datetime 列——已在 `_build_payload_at` 加防御性补列。

验证：17/17 断言（端到端回测 40 次调用预算/5 笔成交/净额与费用/无残留持仓；资金费 3 epoch 扣费/不重复扣）；uvicorn 冒烟回测 API（含 400 参数校验）+ 前端静态服务通过；全量 py_compile 通过。

## 第五轮：深度审计修复（20 项断言全过）

用户要求"再仔细检查还有什么问题"，审计发现并已修复：

1. **trades.json 持久化加固（防资金记录丢失）**：旧 `_save_state` 直接覆写——写入中途崩溃 → 文件截断 → `_load_state` 静默回退默认 = 全部交易记录+配置无声丢失。已改 tmp+fsync+os.replace 原子写入；损坏文件加载时先改名备份（.bak.时间戳）再初始化并 error 级告警。
2. **实盘平仓失败不再标记已平仓（防裸仓，最严重）**：止损/TP1/终极止盈/手动平仓此前不检查 `_execute_live_market_close` 返回值——交易所平仓失败（网络/拒单）时本地照样标记 closed，真实仓位留在交易所且引擎停止跟踪 = 完全裸仓。新增 `_try_live_close`：失败 → 保持原状态继续跟踪、每单每腿推送一次高危警告、下一 tick 自动重试；成功才清标记并撤保护单。手动平仓失败向界面返回 error。
3. **撤单失败同理加固**：手动撤单失败返回 error；自动替换/TTL/熔断撤单失败保持 pending 继续跟踪（本地标 cancelled 的实盘单会停止 sync，可能在交易所隐形成交）。
4. **小修 ×3**：`test_exchange_api` 余额同步键名 `account_balance`→`live_account_balance`（update_config 只写已存在键，原写法被静默丢弃）；`datetime.utcfromtimestamp`（3.12+ 弃用）→ `fromtimestamp(tz=timezone.utc)`；`get_dashboard_data/get_trades` 包装 RLock。

验证：20/20 断言（原子写/损坏备份/平仓失败保持跟踪+告警一次+重试成功/手动平仓撤单失败返回错误/纸盘回归）；uvicorn 冒烟通过。

## 第六轮：重新打包 macOS App（含启动守卫修复）

用户要求重新打包。`python build_app.py` 三次构建均成功（约 45s/次），产物 `dist/FeiyangAgent.app`（140MB，已签名）。验证：PYZ 归档内全部 7 个 backend 模块（含新 backtest）收编、前端为最新构建（含回测面板）。

**重要发现与修复**：启动测试发现用户机器上另一个项目副本 `~/Desktop/demo/btcfy/dist/FeiyangAgent.app`（PID 29741，周三起运行）长期占用 8000 端口。原启动逻辑在 uvicorn 绑定失败后，健康检查会命中旧实例→窗口显示旧后端，而 uvicorn lifespan 已在绑定前启动了监控线程→双实例并行诊断/交易（双倍 LLM 消耗与风险）。修复 `main.py`：启动前 socket 预检 8000 端口，被占则以退出码 1 干净退出并打印中文指引；线程死亡与 10s 超时同样中止。已验证守卫生效（exit 1 + 明确提示）。

**用户待办**：运行新 app 前须先退出 btcfy 旧实例（其 trades 数据在 btcfy/dist/ 下，互不影响）；新 app 数据目录为 FeiyangAgent/dist/（config.yaml/.env/trades.json 沿用该目录下已有文件）。

## 第七轮：熔断器跨模式误触发修复（用户截图实锤）

用户截图反馈：实盘从未开单却显示"今日已实现盈亏 -8.44 USD"触发熔断，切回模拟盘横幅仍在。**根因**：熔断器单一 `start_balance` 跨模式比较——模拟盘记录基准 $8.45，切实盘后拿实盘余额 $0.01 相减得幻影亏损 -8.44 误触熔断；且熔断锁死无出口。修复（17 项断言全过）：

1. **按模式独立记账**：`start_balance_paper/live`、`halted_paper/live`、`notified_paper/live` 分键；切模式时无当日基准则以当前余额初始化，杜绝跨模式幻影亏损。
2. **熔断撤单只影响当前模式**：`_cancel_all_pending` 增加 is_live 过滤（模拟熔断不再误撤实盘挂单，反之亦然）。
3. **手动解除**：`reset_circuit_breaker()`（清除当前模式熔断 + 以当前余额重建基准）+ `POST /api/sniper/reset-breaker` + 前端横幅「🔓 解除熔断」按钮。
4. **旧格式自动迁移**：旧 daily 的 `halted` 键在新代码下天然失效——用户升级重启后误触发自动清除，无需手工改数据。

验证：17/17 断言（截图场景复现不误触/真亏损正常熔断/两模式独立/撤单按模式隔离/手动解除/旧格式升级自清/次日复位）；reset API 冒烟通过；前端 build + 第四次 PyInstaller 打包完成（dist/FeiyangAgent.app）。

## 第八轮：策略 Prompt 界面实时编辑

用户需求：页面上实时修改飞扬 Prompt。实现：
1. **agent.py**：Prompt 抽为类属性 `DEFAULT_SYSTEM_PROMPT`；`__init__` 接受 `system_prompt` 覆盖；新增模块函数 `load_system_prompt(root_dir)`——读取 `<root>/feiyang_prompt.txt`（非空即生效，否则回落默认）。Agent 每次分析新建实例 → 保存后下一次诊断即生效，无需重启。
2. **app.py**：`GET /api/prompt`（返回生效中 prompt + is_custom）、`POST /api/prompt`（保存自定义；空串删除覆盖恢复默认；校验 ≥50 字符且必须含 ```json 信号块与 signal_type 字段——防呆，否则信号无法解析）；手动诊断与 1h 循环两处构造点均传 override；backtest.py 默认 analyzer 同步。
3. **SettingsPanel.jsx**：底部新增「🧠 飞扬策略 Prompt 编辑器（高级）」折叠区——加载生效中 Prompt、「自定义生效中」徽标、等宽 textarea、保存/恢复默认按钮、防呆警告提示。

验证：单元断言（默认/覆盖/agent 回落）+ API 冒烟（GET 默认/非法拒 400/过短拒 400/保存生效/恢复默认）全过；前端 build + 第五次 PyInstaller 打包完成。排障：zsh 双引号内反引号会吃掉 ```json 代码块 → API 测试一律写临时 .py 文件执行。

## 第九轮：修复 webview 缓存导致新 UI 不可见（用户截图确认）

用户重启新包后设置页底部仍无 Prompt 编辑器。排查确认：后端/新 bundle/运行实例全部正常——**WKWebView 启发式缓存了旧 index.html**（StaticFiles 默认无缓存头），旧 html 引用旧 JS 导致新 UI 不渲染。双层修复：
1. `app.py` HTTP 中间件：对 `/` 和 `.html` 响应强制 `Cache-Control: no-cache, must-revalidate`（hash 资源仍长缓存）。
2. `main.py` webview URL 加 `?v=<index.html mtime>` 缓存击穿——每次重新构建 mtime 变化即换 cache key，强制加载新页面。
验证：中间件冒烟（no-cache 头 + 正确 bundle 引用）通过；第六次打包完成。**教训：pywebview 桌面应用升级后 UI 不更新，优先怀疑 WKWebView 对 index.html 的启发式缓存。**
